name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - mendeleev
    - Pmw
    - seamm
    - seamm-util
    - seamm-widgets

      # Testing
    - yapf
