name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # SEAMM
  - seamm
  - pyyaml

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    # Integration testing
    - custom-step
    - from-smiles-step

    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
